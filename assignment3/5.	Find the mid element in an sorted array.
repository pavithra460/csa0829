def element_frequencies(arr):
    freq = {}

    for item in arr:
        if item in freq:
            freq[item] += 1
        else:
            freq[item] = 1

    print("Frequencies of elements:")
    for key, value in freq.items():
        print(f"{key}: {value}")

# Example usage
arr = [4, 2, 4, 5, 2, 3, 1, 4, 3]
element_frequencies(arr)
