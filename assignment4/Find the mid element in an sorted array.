# Sorted array
arr = [2, 4, 6, 8, 10, 12, 14]

# Length of the array
n = len(arr)

# Find mid index
mid_index = n // 2

# Check if length is odd or even
if n % 2 == 1:
    print("Middle element:", arr[mid_index])
else:
    mid_elements = (arr[mid_index - 1], arr[mid_index])
    print("Middle elements:", mid_elements)
