from collections import Counter

def element_frequencies(lst):
    return Counter(lst)

# Example usage
items = [1, 2, 2, 3, 1, 4, 2, 3]
frequencies = element_frequencies(items)
print("Frequencies:", frequencies)
